<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.guojie.tools">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyCoordinator">
        <activity
            android:name=".qrcode.QRCodeActivity"
            android:exported="true" />
        <activity
            android:name=".tabmenu.TabActivity"
            android:exported="true" />
        <activity android:name=".DYLoadingActivity">

            <!-- <intent-filter> -->
            <!-- <action android:name="android.intent.action.MAIN" /> -->


            <!-- <category android:name="android.intent.category.LAUNCHER" /> -->
            <!-- </intent-filter> -->
        </activity>
        <activity android:name=".guidepage.GuideActivity">
        </activity>
        <activity android:name=".BottomMenusViewActivity" />
        <activity android:name=".coordinatorLayout.Coordinator1Acitvity" />
        <activity android:name=".coordinatorLayout.CoordinatorActivity" />
        <activity android:name=".headerviewadapter.MyRecyclerViewActivity" />
        <activity android:name=".banner.BannerActivity" />
        <activity android:name=".BottomSheetDialogActivity" />
        <activity android:name=".percentage.PercentageActivity" />
        <activity
            android:name=".guide.GuideActivity"
            android:screenOrientation="portrait"
            android:theme="@style/NoActivityFullscreen" />
        <activity android:name=".MainActivity" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".letterSide.RightHintActivity" />
    </application>

</manifest>